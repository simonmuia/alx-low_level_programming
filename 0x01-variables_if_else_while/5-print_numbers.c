#include <stdio.h>
#include <math.h>
/**
 * main - entry point
 * Return:0 (successful)
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	printf('\n');
	return (0);
}
