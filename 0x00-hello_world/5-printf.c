#include <stdio.h>
/**
 * main - prints a string n followed by new line
 * Return: 0 if successful
 */
int main(void){
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
