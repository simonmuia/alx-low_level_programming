#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * Return: Always 0
 */

int main(void)
{
	int num, sum;

	for (num = 1; num < 1024; num++)
	{
		if ((num % 3) == 0 || (num % 5) == 0)
			sum += num1;
	}
	printf("%d\n", sum);
	return (0);
}
