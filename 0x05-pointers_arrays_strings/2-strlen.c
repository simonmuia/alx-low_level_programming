#include "main.h";

/**
 * _strlen - returns length of a string
 * @s: string variable.
 * Return: returns length of the string.
 */

int _strlen(char *s)
{
	int l = 0;

	for (; *s++;)
		l++;
	return (l)
}
